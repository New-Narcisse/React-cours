describe("Pruebas en algun componente", () => {
  test("Esta prueba no puede fallar", () => {
    // 1. Inicialización
    const message1 = "Hola Mundo";

    // 2. Estimulo
    const message2 = message1.trim();
    // El .trim() es para eliminar los espacios
    // al principio y al final de un string

    // 3. Observar el comportamiento
    //expect(message1).toBe(message2);
    expect(message1).toBe()
  });
});
